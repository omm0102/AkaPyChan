{
  "ID": "leetcode821",
  "coding_practice": [
    {
      "title": "Shortest Distance to a Character",
      "description": "Given a string s and a character c that occurs in s, return an array of integers answer where answer.length == s.length and answer[i] is the distance from index i to the closest occurrence of character c in s.\n\nThe distance between two indices i and j is abs(i - j), where abs is the absolute value function.",
      "examples": [
        {
          "input": "s = \"loveleetcode\", c = \"e\"",
          "output": "[3,2,1,0,1,0,0,1,2,2,1,0]",
          "explanation": "The character 'e' appears at indices 3, 5, 6, and 11 (0-indexed).\nThe closest occurrence of 'e' for index 0 is at index 3, so the distance is abs(0 - 3) = 3.\nFor index 1, it is abs(1 - 3) = 2.\nFor index 4, it is 1 (tie between indices 3 and 5).\nFor index 8, it is abs(8 - 6) = 2."
        },
        {
          "input": "s = \"aaab\", c = \"b\"",
          "output": "[3,2,1,0]"
        }
      ],
      "constraints": "1 <= s.length <= 10^4\ns[i] and c are lowercase English letters\nIt is guaranteed that c occurs at least once in s",
      "solution": "class Solution:\n    def shortestToChar(self, s, c):\n        n = len(s)\n        res = [n] * n\n        prev = -n\n        for i, ch in enumerate(s):\n            if ch == c:\n                prev = i\n            res[i] = i - prev\n        prev = 2 * n\n        for i in range(n - 1, -1, -1):\n            if s[i] == c:\n                prev = i\n            res[i] = min(res[i], prev - i)\n        return res",
      "explanation": "1. 初始化 res 為長度 n 的大值列表。\n2. 從左到右遍歷 s，遇到字符 c 記錄 prev，res[i] = i - prev 表示距離左邊最近的 c。\n3. 從右到左遍歷 s，更新 res[i] = min(res[i], prev - i)，確保距離最近的 c。\n4. 返回 res。",
      "tag": "611",
      "difficult": "easy"
    }
  ]
}
