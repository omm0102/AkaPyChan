{
  "ID": "leetcode5",
  "coding_practice": [
    {
      "title": "Longest Palindromic Substring",
      "description": "Given a string s, return the longest palindromic substring in s.",
      "examples": [
        {
          "input": "s = 'babad'",
          "output": "'bab'",
          "explanation": "Explanation: \"aba\" is also a valid answer."
        },
        {
          "input": "s = 'cbbd'",
          "output": "'bb'"
        }
      ],
      "constraints": "1 <= s.length <= 1000\ns consist of only digits and English letters.",
      "solution": "class Solution(object):\n    def longestPalindrome(self, s):\n        \"\"\"\n        :type s: str\n        :rtype: str\n        \"\"\"\n        res = \"\"\n        for i in range(len(s)):\n            tmp = self.expandFromCenter(s, i, i)\n            if len(tmp) > len(res):\n                res = tmp\n\n            tmp = self.expandFromCenter(s, i, i + 1)\n            if len(tmp) > len(res):\n                res = tmp\n        return res\n\n    def expandFromCenter(self, s, left, right):\n        while left >= 0 and right < len(s) and s[left] == s[right]:\n            left -= 1\n            right += 1\n        return s[left + 1:right]",
      "explanation": "1. 使用中心擴展法 (Expand Around Center) 來尋找回文子字串。\n2. 對每個字元 i，分別以單字元及雙字元為中心進行擴展。\n3. 擴展時比較左右字元是否相等，直到不相等或越界。\n4. 每次擴展後更新最長回文子字串結果。\n5. 最終返回長度最長的回文子字串。",
      "tag": "511",
      "difficult": "medium"
    }
  ]
}
