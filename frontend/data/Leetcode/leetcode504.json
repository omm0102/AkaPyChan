{
  "ID": "leetcode504",
  "coding_practice": [
    {
      "title": "Base 7",
      "description": "Given an integer num, return a string of its base 7 representation.",
      "examples": [
        {
          "input": "100",
          "output": "\"202\""
        },
        {
          "input": "-7",
          "output": "\"-10\""
        }
      ],
      "constraints": "-10^7 <= num <= 10^7",
      "solution": "class Solution:\n    def convertToBase7(self, num):\n        if num == 0:\n            return \"0\"\n        neg = num < 0\n        num = abs(num)\n        digits = []\n        while num > 0:\n            digits.append(str(num % 7))\n            num //= 7\n        if neg:\n            digits.append('-')\n        return ''.join(digits[::-1])",
      "explanation": "1. 判斷 num 是否為 0，若是直接返回 \"0\"。\n2. 記錄數字是否為負數，並取絕對值處理。\n3. 使用 while 迴圈對 num 取 7 的餘數，將每位數字加入列表 digits。\n4. 將 digits 反轉並合併成字串，如果原數字為負，前面加上 '-'。\n5. 返回最終 base 7 字串。",
      "tag": "511",
      "difficult": "easy"
    }
  ]
}
