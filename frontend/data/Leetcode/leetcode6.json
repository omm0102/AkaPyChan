{
  "ID": "leetcode6",
  "coding_practice": [
    {
      "title": "ZigZag Conversion",
      "description": "Convert a string to a zigzag pattern and read line by line.",
      "examples": {
        "input": "s = 'PAYPALISHIRING', numRows = 3",
        "output": "'PAHNAPLSIIGYIR'"
      },
      "solution": "class Solution:\n    def convert(self, s: str, numRows: int) -> str:\n        if numRows == 1 or numRows >= len(s):\n            return s\n        res = [''] * numRows\n        index, step = 0, 1\n        for c in s:\n            res[index] += c\n            if index == 0:\n                step = 1\n            elif index == numRows - 1:\n                step = -1\n            index += step\n        return ''.join(res)",
      "explanation": "1. 建立 numRows 個空字串儲存每行\n2. 遍歷 s，沿著上下方向添加字符\n3. 最後合併結果",
      "tag": "611",
      "difficult": "easy"
    }
  ]
}
