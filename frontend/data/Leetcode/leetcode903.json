{
  "ID": "leetcode903",
  "coding_practice": [
    {
      "title": "Valid Permutations for DI Sequence",
      "description": "You are given a string s of length n where s[i] is either:\n\n'D' means decreasing, or\n'I' means increasing.\nA permutation perm of n + 1 integers of all the integers in the range [0, n] is called a valid permutation if for all valid i:\n\nIf s[i] == 'D', then perm[i] > perm[i + 1], and\nIf s[i] == 'I', then perm[i] < perm[i + 1].\nReturn the number of valid permutations perm. Since the answer may be large, return it modulo 10^9 + 7.",
      "examples": [
        {
          "input": "s = \"DID\"",
          "output": "5",
          "explanation": "The 5 valid permutations of (0, 1, 2, 3) are: (1,0,3,2), (2,0,3,1), (2,1,3,0), (3,0,2,1), (3,1,2,0)"
        },
        {
          "input": "s = \"D\"",
          "output": "1"
        }
      ],
      "constraints": "n == s.length\n1 <= n <= 200\ns[i] is either 'I' or 'D'",
      "solution": "class Solution(object):\n    def numPermsDISequence(self, S):\n        MOD = 10 ** 9 + 7\n        n = len(S)\n        dp = [1] * (n+1)\n        for i, ch in enumerate(S):\n            ndp = [0] * (n+1)\n            if ch == 'D':\n                total = 0\n                for j in reversed(range(n-i)):\n                    total = (total + dp[j+1]) % MOD\n                    ndp[j] = total\n            else:\n                total = 0\n                for j in range(n-i):\n                    total = (total + dp[j]) % MOD\n                    ndp[j] = total\n            dp = ndp\n        return sum(dp[:n]) % MOD",
      "explanation": "1. dp[i][j] 表示長度為 i 的序列以數字 j 結尾的有效排列數。\n2. 使用前綴和優化計算，每次根據 'I' 或 'D' 遞推新的 dp。\n3. 'I' 往左累加，'D' 往右累加，確保遞推公式正確。\n4. 最後 sum(dp[:n]) 取得長度 n+1 的所有排列數，對 10^9+7 取模。",
      "tag": "723",
      "difficult": "hard"
    }
  ]
}
