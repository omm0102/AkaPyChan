{
  "ID": "leetcode90",
  "coding_practice": [
    {
      "title": "Subsets II",
      "description": "Given an integer array nums that may contain duplicates, return all possible subsets (the power set).\n\nThe solution set must not contain duplicate subsets. Return the solution in any order.",
      "examples": [
        {
          "input": "nums = [1,2,2]",
          "output": "[[],[1],[1,2],[1,2,2],[2],[2,2]]"
        },
        {
          "input": "nums = [0]",
          "output": "[[],[0]]"
        }
      ],
      "constraints": "1 <= nums.length <= 10\n-10 <= nums[i] <= 10",
      "solution": "class Solution(object):\n    def subsetsWithDup(self, nums):\n        nums.sort()\n        res = [[]]\n        for num in nums:\n            new_subsets = []\n            for subset in res:\n                new_subsets.append(subset + [num])\n            res += new_subsets\n        unique_res = []\n        seen = set()\n        for subset in res:\n            t = tuple(subset)\n            if t not in seen:\n                seen.add(t)\n                unique_res.append(subset)\n        return unique_res",
      "explanation": "1. 將 nums 排序，保證相同元素相鄰，方便後續去重。\n2. 使用迭代方式生成所有子集：對每個元素 num，將其加到已存在的每個子集中，生成新子集。\n3. 對生成的所有子集進行去重：使用集合記錄已出現的子集，確保結果不包含重複子集。",
      "tag": "613",
      "difficult": "medium"
    }
  ]
}
