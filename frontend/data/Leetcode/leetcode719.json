{
  "ID": "leetcode719",
  "coding_practice": [
    {
      "title": "Find K-th Smallest Pair Distance",
      "description": "The distance of a pair of integers a and b is defined as the absolute difference between a and b.\\nGiven an integer array nums and an integer k, return the kth smallest distance among all the pairs nums[i] and nums[j] where 0 <= i < j < nums.length.",
      "examples": [
        {
          "input": "nums = [1,3,1], k = 1",
          "output": "0",
          "explanation": "Here are all the pairs: (1,3) -> 2, (1,1) -> 0, (3,1) -> 2. Then the 1st smallest distance pair is (1,1), and its distance is 0."
        },
        {
          "input": "nums = [1,1,1], k = 2",
          "output": "0"
        },
        {
          "input": "nums = [1,6,1], k = 3",
          "output": "5"
        }
      ],
      "constraints": "n == nums.length\\n2 <= n <= 10^4\\n0 <= nums[i] <= 10^6\\n1 <= k <= n * (n - 1) / 2",
      "solution": "class Solution:\n    def smallestDistancePair(self, nums, k):\n        nums.sort()\n        \n        def count_pairs(mid):\n            count = 0\n            left = 0\n            for right in range(len(nums)):\n                while nums[right] - nums[left] > mid:\n                    left += 1\n                count += right - left\n            return count\n        \n        left, right = 0, nums[-1] - nums[0]\n        while left < right:\n            mid = (left + right) // 2\n            if count_pairs(mid) < k:\n                left = mid + 1\n            else:\n                right = mid\n        return left",
      "explanation": "1. 對 nums 進行排序。\\n2. 使用二分搜尋尋找距離 mid，使得小於等於 mid 的對數量至少為 k。\\n3. count_pairs(mid) 計算小於等於 mid 的對數量，使用雙指標方法。\\n4. 最終 left 會是第 k 小的距離。",
      "tag": "1030",
      "difficult": "hard"
    }
  ]
}
