{
  "ID": "leetcode852",
  "coding_practice": [
    {
      "title": "Peak Index in a Mountain Array",
      "description": "You are given an integer mountain array arr of length n where the values increase to a peak element and then decrease.\n\nReturn the index of the peak element.\n\nYour task is to solve it in O(log(n)) time complexity.",
      "examples": [
        {
          "input": "arr = [0,1,0]",
          "output": "1"
        },
        {
          "input": "arr = [0,2,1,0]",
          "output": "1"
        },
        {
          "input": "arr = [0,10,5,2]",
          "output": "1"
        }
      ],
      "constraints": "3 <= arr.length <= 10^5\n0 <= arr[i] <= 10^6\narr is guaranteed to be a mountain array.",
      "solution": "class Solution(object):\n    def peakIndexInMountainArray(self, arr):\n        \"\"\"\n        :type arr: List[int]\n        :rtype: int\n        \"\"\"\n        left, right = 0, len(arr) - 1\n        \n        while left < right:\n            mid = left + (right - left) // 2\n            if arr[mid] < arr[mid + 1]:\n                left = mid + 1\n            else:\n                right = mid\n        \n        return left",
      "explanation": "1. 使用二分搜尋法尋找峰值。\n2. 設定 left = 0, right = len(arr)-1。\n3. 計算 mid，如果 arr[mid] < arr[mid+1]，表示峰值在右側，left = mid + 1；否則，峰值在左側或 mid，本輪 right = mid。\n4. 最終 left 等於 right，即峰值索引，回傳 left。",
      "tag": "520",
      "difficult": "medium"
    }
  ]
}
