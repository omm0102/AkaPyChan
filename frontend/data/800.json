{
  "ID": 800,
  "coding_practice": [
    {
      "title": "計算程式執行時間",
      "description": "請使用 time 模組，寫一個函數 measure_time(func, *args)，用來測量函數 func 執行所需的秒數，並回傳結果 (四捨五入到小數點 3 位)。",
      "examples": {
        "input": "import time\n\ndef slow_add(a, b):\n    time.sleep(2)\n    return a + b\n\nprint(measure_time(slow_add, 3, 5))",
        "output": "約 2.0 秒 (依電腦執行環境略有差異)"
      },
      "solution": "import time\n\ndef measure_time(func, *args):\n    start = time.time()\n    func(*args)\n    end = time.time()\n    return round(end - start, 3)\n\n# 測試\nimport time\n\ndef slow_add(a, b):\n    time.sleep(2)\n    return a + b\n\nprint(measure_time(slow_add, 3, 5))  # 約 2.0"
    },
    {
      "title": "自訂模組：攝氏與華氏轉換",
      "description": "請建立一個模組區塊，內含兩個函數：\n1. c_to_f(c)：將攝氏溫度 c 轉換為華氏溫度。\n2. f_to_c(f)：將華氏溫度 f 轉換為攝氏溫度。\n\n接著在主程式區塊中使用 input() 讀取使用者輸入：\n第一行輸入 'C' 或 'F' 表示轉換方向；第二行輸入數值，並印出結果。",
      "examples": {
        "input": "C\n0",
        "output": "32.0"
      },
      "solution": "# 模組區塊\ndef c_to_f(c):\n    return c * 9 / 5 + 32\n\ndef f_to_c(f):\n    return (f - 32) * 5 / 9\n\n# 主程式區塊\ntype_ = input().strip().upper()  # 讀取轉換方向 (C 或 F)\nvalue = float(input())            # 讀取溫度值\n\nif type_ == 'C':\n    print(c_to_f(value))\nelif type_ == 'F':\n    print(f_to_c(value))\nelse:\n    print('輸入錯誤，請輸入 C 或 F')"
    }
  ]
}
